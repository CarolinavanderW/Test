pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hola Carol"'
                sh 'pwd'
                sh 'mkdir test-multibranch'
                
            }
        }
    }
}
